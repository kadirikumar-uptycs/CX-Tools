let isAuthenticated = require('./isAuthenticated');
let isDomainAllowed = require('./isDomainAllowed');
let isAdmin = require('./isAdmin');

module.exports = {
    isAuthenticated,
    isDomainAllowed,
    isAdmin
}